## Logging
log.console = true
log.console.level = info

## Management plugin
management.tcp.port = 15672

## Default credentials and vhost are set via environment variables:
## RABBITMQ_DEFAULT_USER, RABBITMQ_DEFAULT_PASS, RABBITMQ_DEFAULT_VHOST
