framework:
    rate_limiter:
        login_throttle:
            policy: sliding_window
            limit: 5
            interval: '60 seconds'
        refresh_throttle:
            policy: sliding_window
            limit: 10
            interval: '60 seconds'
